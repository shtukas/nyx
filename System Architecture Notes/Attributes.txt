
Attributes

Attribues are named values that can be attached to objects. The implementation ensures that we can retrieve all the UUIDs for a given attribute name / value pair. 

create table _attributes_ (_uuid_ text, _attributename_ text, _attributevalue_ text);
