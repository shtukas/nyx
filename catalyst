#!/usr/bin/ruby

# encoding: UTF-8

require_relative "Libs/loader.rb"

# ----------------------------------------------------------------------------

AFewNx50s::initialise(false)

Thread.new {
    AFewNx50s::initialise(true)
}

Thread.new {
    loop {
        sleep 300
        system("#{File.dirname(__FILE__)}/vienna-import")
    }
}

TerminalDisplayOperator::displayLoop()
