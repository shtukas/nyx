
Type:
    "uuid"           : UUID
    "mikuType"       : mikuType
    "unixtime"       : Float
    "datetime"       : DateTime Iso 8601 UTC Zulu
    "description"    : String
    "payload"        : 
    "doNotShowUntil" : Unixtime

    "field1"         : string, (used, miku type specific)
    "field2"         : string, (used, miku type specific)
    "field3"         : string, (used, miku type specific)
    "field4"         : string, (used, miku type specific)
    "field5"         : string, (used, miku type specific)
    "field6"         : string, (used, miku type specific)

    "field7"         : string, skipped until # unixtime
    "field8"         : string, lockname      # string

    "field9"         : string, (not used)

    "field10"        : string, (not used)
    "field11"        : string, (not used)

    "field12"        : string, "true" if listed
    "field13"        : float , listing ordinal

```
TodoDatabase2::filepaths().each{|filepath|
    db = SQLite3::Database.new(filepath)
    db.busy_timeout = 117
    db.busy_handler { |count| true }
    db.results_as_hash = true
    db.execute "alter table objects add column field12 text", []
    db.close
}
```