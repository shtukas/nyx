
Type:
    "uuid"           : UUID
    "mikuType"       : mikuType
    "unixtime"       : Float
    "datetime"       : DateTime Iso 8601 UTC Zulu
    "description"    : String
    "payload"        : 
    "doNotShowUntil" : Unixtime

    "field1"         : (used, miku type specific)
    "field2"         : (used, miku type specific)
    "field3"         : (used, miku type specific)
    "field4"         : (used, miku type specific)
    "field5"         : (used, miku type specific)
    "field6"         : (used, miku type specific)

    "field7"         : skipped until # unixtime
    "field8"         : lockname      # string
    "field9"         : fiberId       # string

    "field10"        : (not used)
    "field11"        : (not used)
    "field12"        : (not used)

```
TodoDatabase2::filepaths().each{|filepath|
    db = SQLite3::Database.new(filepath)
    db.busy_timeout = 117
    db.busy_handler { |count| true }
    db.results_as_hash = true
    db.execute "alter table objects add column field12 text", []
    db.close
}
```