
Nx5 Files are sqlite database that implement two tables

create table events (_eventuuid_ text primary key, _eventTime_ float, _eventType_ text, _event_ text);
create table datablobs (_nhash_ text primary key, _datablob_ blob);

The _events_ tables carries Nx3s
