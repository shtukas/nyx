
We do not have a shared mercury channel, so we use those specific objects to record and signify that a Dx8Unit needs to be garbage collected.

{
    ... Stargate Item ("Dx33")
    "unitId"   => String # Unit Id of the Dx8 to be garbage collected
}
