
DxF1s are log of events, some are attributes values, some are datablobs. They represent the nodes of the stargate network.

Databases:
    - Bank
    - DoNotShowUntil
    - item-to-group-mapping.sqlite3
    - network-links.sqlite3

The index
  
create table _index_ (_objectuuid_ text primary key, _unixtime_ float, _mikuType_ text, _item_ text, _dxf1_ text)

When we update an item
    1. Find the _dxf1_ item, either in local or in the vaults
    2. Update the _dxf1_ while updating its name to match the new contents
    3. Update the 
