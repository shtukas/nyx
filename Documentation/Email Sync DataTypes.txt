
# -----------------------------------------
EmailTrackingClaim
{
    "emailuid"                 : String
    "genericContentsItemUUID"  : String
    "streamItemUUID"           : String
    "creationUnixtime"         : Integer
    "status"                   : "init" | "detached" | "deleted-on-server" | "deleted-on-local" | "dead"
    "lastStatusUpdateUnixtime" : Integer
}

"detached" : for when an email has been recast or datecoded, they are deleted from the server and operate as standard Stream items.

